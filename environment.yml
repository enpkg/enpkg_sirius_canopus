name: sirius_canopus_single_files
channels:
  - defaults
  - conda-forge
  - anaconda
dependencies:
  - python=3.8
  - ipykernel
  - numpy >= 1.0.0
  - pandas >= 0.24.0
  - scikit-learn >= 0.20.3
  - scipy >= 1.2.1
  - seaborn >= 0.9.0
  - matplotlib >= 3.0.0
  - pyyaml
  - tqdm

prefix: \opt\anaconda3\envs\sirius_canopus_single_files
